{{# each products}}
  <div class="row">
    {{# each this}}
    <div class="col-sm-6 col-md-4">
      <div class="thumbnail">
        <a href = "/products/{{this._id}}">
        <img src="{{this.image}}" alt="...">
        <div class="caption">
          <h3>{{this.name}}</h3>
          </div>
          </a>
      </div>
      <div class ="clearfix">
        <div class="price pull-left">{{this.price}}VNĐ</div>
        <a href="/add-to-cart/{{this._id}}" class="btn btn-success pull-right" role="button">Add to Shopping</a>
      </div>
    </div>
    {{/each}}
  </div>
{{/each}}